language: minimal

services:
  - docker

before_install:
  - ./ci/ci.sh detect_version

script:
  - export LHPROXY_VERSION="$(cat build/version.txt)"
  - ./ci/ci.sh build
  # - ./ci/ci.sh fmt

deploy:
  provider: releases
  name: test
  overwrite: true
  edge: true
